providers:
  docker:
    exposedByDefault: false
    network: "proxy"
  file:
    filename: "/etc/traefik/dynamic_conf.yaml"
    watch: true

api:
  dashboard: true
  insecure: false

log:
  level: "INFO"

accessLog: {}

metrics:
  prometheus: {}
